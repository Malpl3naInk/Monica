# 本轮设计检查记录

2026-09-20，设计阶段只处理方案文档、M3E Canvas JSON/链接、渲染预览及其生成工具。用户后来追加的 Release 构建排障单独记录在下方。

## 已完成

- 只读核对当前 Android 存储实体、类型转换、批量创建、主要后端写入与既有 Token 附加资料机制；另核对工作区 MDBX/CLI 限制定义与调用路径。
- 生成 5 张可编辑 M3E Canvas、20 个状态，保留压缩 URL 和可导入 JSON。
- 核对每个压缩链接解码后与 JSON 一致、组件 ID 无重复、文档本地链接与图片存在；两篇笔记样例的编辑目标指向原项目。
- 在真实 M3E Canvas 网页中逐张打开，所有画布文案出现、无页面脚本错误；结果见 [render-check.json](canvas/render-check.json)。
- 目视检查新建、共用详情/笔记、专用内容、批量创建、存储与冲突五张总览；检查新建单页明暗色、标题折叠与旧类型选择的修订。
- QR 占位初稿显示过小，已改成占据正常出示区域的示意矩阵，重新渲染对应画布。矩阵不是真实可用二维码。
- 旧 new-entry-v2 保留，按用户最新反馈更新全部涉及的顶栏切换控件、正文类型行和收起标题。7 张旧画布的 28 个状态已重新渲染。
- 准确区分 Android/CLI Token 的 16 KiB 限制、Android 附加资料的 64 KiB 限制、MDBX 通用读写预算；没有把它们误说成数据库总容量。
- 整理 [需要审阅的取舍](decisions.md)，明确哪些候选可能触及 MDBX 源码及判断条件。

## 设计阶段的验证边界

没有修改应用功能代码、Room schema、MDBX 源码或 CLI；没有启动 AVD、迁移真实数据、测试真实账号或提交发布。下方对现有应用的打包验证不代表统一项目方案已实现或通过功能测试。

本轮检查只证明设计文档与 Canvas 能正确打开。Compose 折叠动效、焦点/键盘、读屏、大字体、Rust 性能、数据往返、崩溃恢复和降级编辑均需后续实施验收，不能由静态稿认定通过。

工作区原有的其他应用源码改动不属于本轮设计，不覆盖、不回滚，也不把它们纳入本轮验证结论。

## 用户追加的 Release 构建排障

- 用户提供的 `compileReleaseKotlin` 日志共 358 条错误，集中在 `TotpCodeCard`、`AddEditPasswordScreen`、`GeneratorScreen`、`GpgKeyScreen`；大量错误是顶层函数未解析后的连锁报错。
- 核对相关定义、包名与 source set，函数和组件均存在。Gradle 检测到上次编译失败，完整重建 Kotlin 输出后，这批错误全部消失。证据指向旧增量编译状态不一致，具体形成原因未从日志中确定。
- 没有为此补造组件、修改业务源码、升级工具链、关闭后续增量编译或更改版本号，也没有清理全局 Gradle 缓存。
- 执行 `./gradlew.bat :app:assembleRelease --offline --console=plain --no-daemon --max-workers=2`，结果为 **BUILD SUCCESSFUL**，耗时 18 分 11 秒；69 个任务，14 个执行、55 个为最新状态。
- Kotlin 编译、Java 编译、R8、资源压缩与 APK 打包均通过。仍有既有的弃用/实验 API 和 R8 Kotlin metadata 兼容警告；这次未扩展为工具链升级或运行时验收。
- 未新增或运行与本次编译问题无关的测试，未启动虚拟机，也未额外交付安装包。

本次构建日志位于 `C:/Users/joyins/AppData/Local/Temp/monica-release-rebuild-20260920.log`。

## v2：根据截图缩小模板和追加按钮

- 模板改为大标题下方紧凑居左的名称＋箭头按钮组；追加 URL／应用、内容、笔记、附件等按钮按文案宽度缩小，视觉高度 40dp。保存 FAB、字段宽度和标题滚动方案保持不变。
- 旧 new-entry-v2 的相关控件同步修订为 v6。两套共 12 张画布、48 个状态重新通过真实 M3E Canvas 渲染，文字检查无缺失、页面脚本无错误。
- 目视检查新建首屏明暗色及下滑状态；[最终预览](canvas/compact-controls-preview.png)裁自这些实际渲染图。新链接与 JSON 保留可编辑性。
- 至少 48dp 触控区域、长译文换行和圆角按压裁剪是后续原生实现约束，不能由静态 Canvas 当作已实现。
- 本次尺寸修订只改设计文档、画布及其生成脚本，没有运行 Android 构建/测试或修改应用源码。
