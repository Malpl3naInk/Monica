# 本轮设计检查

- 本轮仅新增 docs/new-entry-v2 下的设计文件；应用源码未修改。
- GitHub API 查询的 Keyguard HEAD 与独立参考目录 HEAD 一致：7e124b57189eb87fcff85c8a0b51d4debfa440b1。
- 5 张 M3E Canvas（20 个状态画面）在 Microsoft Edge 中实际打开与渲染，全部文案存在、无页面脚本错误。每张保留可编辑链接和 JSON 备份。
- 已目视检查 5 张总览：密码流程、凭据、卡包、专用编辑器、实验设置/保存状态；另检查密码默认页明暗预览。
- 初次导入因装饰组件缺少 Canvas 必填 label 属性被拒绝，已修正并重新渲染，当前图片均为成功导入后的画布。
- 预览图是 M3E Canvas 设计稿，不是 Android 运行截图。拖拽动效、键盘焦点、TalkBack、Rust 收益与数据库读写留待实施后验收。
- 本轮没有启动 Gradle/JDK 或 Android 虚拟机，没有构建、安装、付款、提交、推送或修改 CLI/F-Droid 功能代码。

[渲染检查结果](canvas/render-check.json) · [暗色单页](canvas/password-detail.png) · [浅色单页](canvas/password-detail-light.png)

## v2：根据分区多项追加反馈修订

取消全局添加 bottom sheet；网站、自定义字段、附件各区保留可反复使用的添加更多。核心画布重画为空区、两个网站/两个自定义字段、多附件及逐项菜单；其他资料降为页尾文字入口。其余首屏图以草图注释标明向下接续各自追加区，该注释不属于产品 UI。

5 张 Canvas 已重新打开渲染，20 个画面文案检查通过、无页面脚本错误；已目视核查新的四画面密码流程。此轮只修改设计文件，应用源码哈希保持一致。预览展示空区与已有多项两个状态，不是两个不同页面。

## v3：明确 URL 操作与逐项菜单

新增 URL 双按钮组（外侧 28dp、内侧 8dp、中缝 4dp），将 URL 区追加操作改名为添加 URL / 绑定应用；重复卡片右侧三点固定显示。新增四个菜单状态画面，当前共 6 张画布、24 个画面。全部重新渲染，文案检查通过、无页面脚本错误；应用源码哈希未改变。

## v4：专门设计类型切换

取消标题旁下拉箭头。统一顶栏类型图标/名称与切换入口，补充六种密码子类型面板、切到 SSH 和滚动固定状态，已目视检查对应画布。现为 7 张可编辑 Canvas、28 个画面，重新渲染的文案检查全部通过，无页面脚本错误。窄屏/大字体双行顶栏及草稿恢复为原生实施验收要求，本轮没有声称原生通过。应用源码哈希未改变。

## v5：根据最新反馈移动选择控件

v4 的固定顶栏类型控件已被替换：类型选择是一整行正文选项，放在大标题下；滚动时选择行离开视口，大标题收进顶栏。所有受影响的旧画布同步更新，7 张画布/28 个状态已重新在 M3E Canvas 渲染，文案检查无缺失、页面脚本无错误。

当前预览：[标题下的类型选择](canvas/type-below-title.png)、[滚动后的标题](canvas/collapsed-title.png)。新建统一项目的模板使用相同布局，见 [新方案](../unified-items-v1/README.md)。这次仅更新设计文件；未运行 Android 构建/测试，不将原生动效与布局说成已经实现。

## v6：紧凑居左的选择和追加按钮

按用户新的 Keyguard 截图修订 v5：类型名称＋箭头组成紧凑按钮组，放在大标题下方并靠左；URL／应用及各区追加按钮同样缩小，视觉高度 40dp，宽度随文案。字段与保存位置保持原样，标题仍随滚动收进顶栏。

7 张画布、28 个状态已重新通过真实 M3E Canvas 渲染，文字检查无缺失、无页面脚本错误。触控区至少 48dp、字体放大及长译文换行规则已写入方案，尚未接入原生代码。本次没有运行 Android 构建或测试。
